[project]
name = "zermelo-mcp"
version = "0.1.0"
dependencies = [
    "zermelo",
    "mcp[cli]",
]

[tool.uv.sources]
zermelo = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
